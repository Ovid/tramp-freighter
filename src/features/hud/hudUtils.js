import { calculateDistanceFromSol } from '../../game/constants';

/**
 * Re-export calculateDistanceFromSol from constants.js for HUD components.
 *
 * This provides a convenient import path for HUD utilities while maintaining
 * a single source of truth for the distance calculation logic.
 */
export { calculateDistanceFromSol };
